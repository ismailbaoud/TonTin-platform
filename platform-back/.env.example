# =============================================================================
# TonTin Platform Backend – example environment variables
# Copy to .env and fill in your values. Do not commit .env (it contains secrets).
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_DB=tontin_test
POSTGRES_USER=your-db-user
POSTGRES_PASSWORD=your-db-password

# Spring DataSource (local: localhost:5432; Docker: postgres:5432)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/tontin_test
SPRING_DATASOURCE_USERNAME=your-db-user
SPRING_DATASOURCE_PASSWORD=your-db-password

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
SERVER_PORT=8080

# -----------------------------------------------------------------------------
# JWT (use a strong random secret in production, e.g. 32+ hex chars)
# -----------------------------------------------------------------------------
SECURITY_JWT_SECRET_KEY=your-256-bit-hex-secret
SECURITY_JWT_ISSUER=tontin-platform
SECURITY_JWT_ACCESS_TOKEN_EXPIRATION=900000
SECURITY_JWT_REFRESH_TOKEN_EXPIRATION=2592000000
SECURITY_PASSWORD_BCRYPT_STRENGTH=10

# -----------------------------------------------------------------------------
# Stripe (optional – leave empty for mock payment mode)
# Get keys: https://dashboard.stripe.com/apikeys (use test keys for dev)
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
# Webhook: run `stripe listen --forward-to localhost:8080/api/v1/payments/webhook` (local)
# or add endpoint in Dashboard → Webhooks (production) and paste Signing secret (whsec_...)
STRIPE_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Mail (SMTP – for verification/invitation emails)
# Gmail: use App Password from https://myaccount.google.com/apppasswords
# -----------------------------------------------------------------------------
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=your-email@gmail.com
SPRING_MAIL_PASSWORD=your-app-password

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
ENV=development
SPRING_PROFILES_ACTIVE=default
